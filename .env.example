# ==========================================
# Template for Environment Variables
# Copy this file, rename it to .env, and fill in your actual values.
# DO NOT commit the real .env file to Git!
# ==========================================

# OpenAI API Key (Required for the Agent reasoning and extraction)
OPENAI_API_KEY=your_openai_api_key_here
MODEL_NAME=gpt-4o
# (Optional - for future use) We can add things like server port or model name later
# MODEL_NAME=gpt-4o